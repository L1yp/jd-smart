{
  "config": {
    "step": {
      "user": {
        "title": "JD Smart (小京鱼) — Account credentials",
        "description": "Two steps:\n① **tgt** — login ticket (expires; must be the JD IoT '小京鱼' app's tgt);\n② **device identity** — fill device_id OR android_id (prefer device_id; some phones can't read android_id, and device_id = md5(android_id), the same value).\n\nDiscovery uses the lightweight gw.smart endpoints — **no rainbow-gateway params needed**.\n\nOptional enhancement: fill **pin + jmafinger** (from the app cookie) to enable the full device model (fan speed/mode, numeric steps, read-only electrical streams); without them you only get an on/off (Power) switch + sensors.\n\nAll other params use defaults — tweak under Options → Advanced. The same phone can hold multiple accounts (told apart by pin or home).",
        "data": {
          "tgt": "tgt (login ticket, expires; JD IoT app)",
          "device_id": "device_id (preferred; = md5(android_id))",
          "android_id": "android_id (Settings.Secure.ANDROID_ID, 16hex; or fill device_id instead)",
          "color_pin": "color_pin (optional; with jmafinger enables the full model)",
          "color_jmafinger": "color_jmafinger (optional; cookie UUID)"
        }
      },
      "houses": {
        "title": "Select homes",
        "description": "Choose which homes to pull devices from.",
        "data": {"houses": "Homes"}
      },
      "devices": {
        "title": "Select devices",
        "description": "Choose devices to add. Sensors are built from each device's streams.",
        "data": {"devices": "Devices"}
      }
    },
    "error": {
      "need_identity": "Fill device_id or android_id (at least one). Prefer device_id — it equals md5(android_id).",
      "cannot_connect": "Discovery failed: {detail}. Usually an expired tgt or a network issue — make sure tgt is the latest value from the JD IoT app.",
      "eid_fetch_failed": "Failed to fetch eid: {detail}. Enter an eid manually, or check the device identity/network."
    },
    "abort": {
      "already_configured": "This account is already configured (matched by pin or home). Don't add the same account twice.",
      "cannot_connect": "Discovery failed (network / risk control / credentials). Check tgt and retry.",
      "no_houses": "No homes returned for this account.",
      "no_devices": "No devices found in the selected homes."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "JD Smart options",
        "menu_options": {
          "credentials": "Update credentials (tgt / identity / optional pin+jmafinger)",
          "rediscover": "Re-discover devices",
          "streams": "Edit sensors / units",
          "settings": "Advanced (all parameters)",
          "manual": "Manual device entry"
        }
      },
      "credentials": {
        "title": "Update credentials",
        "description": "Update these (tgt expires most often): tgt login ticket and the device identity — device_id or android_id (fill either, prefer device_id). Optional pin + jmafinger (fill both) unlock the full device model (fan steps/mode, etc.). Everything else stays unchanged; to edit seg1/key/model/eid/interval use Advanced. After updating, run Re-discover if your devices changed.",
        "data": {
          "tgt": "tgt (login ticket, expires; JD IoT app)",
          "device_id": "device_id (preferred; = md5(android_id))",
          "android_id": "android_id (16hex; or fill device_id instead)",
          "color_pin": "color_pin (optional; with jmafinger enables the full model)",
          "color_jmafinger": "color_jmafinger (optional; cookie UUID)"
        }
      },
      "settings": {
        "title": "Advanced (all parameters)",
        "description": "Discovery uses gw (only tgt + device identity); seg1/key poll snapshots/control; pin/jmafinger/color_sign_secret are optional — fill them to fetch the full model via rainbow getDeviceDetails; device_id (or android_id) is the device id; leave eid empty to auto-fetch. Upgrading: fill tgt and (optionally) pin/jmafinger here, then run Re-discover.",
        "data": {
          "color_sign_secret": "color_sign_secret (rainbow HMAC key)",
          "color_pin": "color_pin (cookie pin)",
          "color_jmafinger": "color_jmafinger (cookie UUID)",
          "tgt": "tgt (login ticket)",
          "seg1": "seg1 (constant id)",
          "key": "HmacSHA1 key",
          "android_id": "android_id (16hex; or use device_id)",
          "device_id": "device_id (preferred; = md5(android_id), overrides android_id)",
          "d_brand": "Brand",
          "d_model": "Model",
          "os_version": "OS version (SDK_INT)",
          "screen": "Screen (e.g. 1080*2160)",
          "area": "LBS area code",
          "network_type": "Network type",
          "eid": "eid (optional; empty = auto-fetch)",
          "hard_platform": "hard_platform (model)",
          "app_version": "app_version",
          "plat_version": "plat_version",
          "channel": "channel",
          "plat": "plat",
          "scan_interval": "Scan interval (seconds)"
        }
      },
      "rediscover": {"title": "Select homes", "data": {"houses": "Homes"}},
      "redevices": {"title": "Select devices", "data": {"devices": "Devices"}},
      "streams": {
        "title": "Edit sensors",
        "description": "Pick a device to edit its streams.",
        "data": {"device": "Device"}
      },
      "edit_device": {
        "title": "Streams of {device}",
        "description": "For each stream set name__ / unit__ / enabled__. The field suffix (the part after name__/unit__/enabled__) is the Param ID in the table below; card_desc units may be inaccurate — fix them against the original parameters below.\n\n| Param ID | Original name | Original unit | Options |\n|---|---|---|---|{reference}"
      },
      "manual": {
        "title": "Manual devices",
        "description": "One per line: name|feed_id. device_id comes from the account identity (device_id, or md5(android_id)).",
        "data": {"manual": "Devices (name|feed_id, one per line)"}
      }
    },
    "abort": {
      "cannot_connect": "Discovery failed. Check tgt and retry.",
      "no_houses": "No homes returned for this account.",
      "no_devices": "No devices cached. Use Re-discover first.",
      "missing_device_config": "Missing tgt or device identity (device_id/android_id). Open 'Update credentials' (or Advanced) and fill them first."
    }
  },
  "services": {
    "get_device_snapshot": {
      "name": "Query device snapshot",
      "description": "Call getDeviceSnapshot_v1 and return the device status snapshot.",
      "fields": {
        "device_id": {
          "name": "device_id",
          "description": "Device id used in the query string."
        },
        "feed_id": {
          "name": "feed_id",
          "description": "Device feed_id used in the body."
        }
      }
    },
    "control_device": {
      "name": "Control device",
      "description": "Call controlDevice_v1 to send commands; returns the updated streams.",
      "fields": {
        "feed_id": {
          "name": "feed_id",
          "description": "Target device feed_id (already selected in the integration)."
        },
        "stream_id": {
          "name": "stream_id",
          "description": "Stream id to control (single-write form, paired with value)."
        },
        "value": {
          "name": "value",
          "description": "Code to write (single-write form). Switch 0/1; enum step code; number for numeric streams."
        },
        "command": {
          "name": "command (batch)",
          "description": "Raw command array to write multiple streams at once: [{stream_id, current_value}, ...]."
        }
      }
    },
    "get_device_model": {
      "name": "Query device model (diagnostic)",
      "description": "Fetch the device's rainbow getDeviceDetails model; returns raw response + parsed model + control routing. Use it to debug 'why only a Power switch' — a control_map missing Mode/Wind/oscillation means getDeviceDetails returned no model at runtime and it silently fell back to card_meta (whose card_control only marks Power controllable).",
      "fields": {
        "feed_id": {
          "name": "feed_id",
          "description": "Target device feed_id (already selected in the integration)."
        }
      }
    }
  }
}
